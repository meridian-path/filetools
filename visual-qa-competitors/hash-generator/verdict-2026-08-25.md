# hash-generator: lineup + squint verdict, 2026-08-25

Screenshots: `verdict-2026-08-25-lineup.png`, `verdict-2026-08-25-squint.png` (this directory).
Produced by `npm run lineup-squint -- dist/data/hash-generator/index.html
visual-qa-competitors/hash-generator`, viewed with the `Read` tool before recording this
verdict.

Competitors (real screenshots, non-full-page 1440x900, captured 2026-08-25 via
`scripts/capture-competitor-screenshot.js`): `browserling-md5.png`, `codebeautify-md5.png`,
`emn178-online-tools-sha256.png`, `md5hashgenerator-com.png`.

## Lineup test: **PASS**

Placed in the row next to the four competitors, this repo's own tile reads as a single,
uncluttered card centered on one task (a labeled drop-zone plus one accent-filled action
button) against a plain background. All four competitors read differently at a glance: one is
mostly obscured by a promo/pricing popup, one carries a dense top-nav-plus-sidebar developer-
tools-directory layout, and two are link-heavy, ad-site-style pages with multi-column sidebars
and small blue underlined text. The contrast in density and focus is real and visible without
having to read any label -- this repo's tile is identifiable as not-a-template.

## Squint test: **PASS**

Blurred, the page still resolves to one dominant shape: the bordered drop-zone card with its
accent button beneath it, roughly centered, high-contrast against the lighter background. The
heading text above it blurs to a soft gray block that reads as secondary, not competing for
attention. One dominant element survives the blur, matching the doctrine's "one dominant
element per view" bar.
