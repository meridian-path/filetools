# hash-generator: lineup + squint verdict, 2026-08-27

Screenshots: `verdict-2026-08-27-lineup.png`, `verdict-2026-08-27-squint.png` (this directory).
Produced by `npm run lineup-squint -- dist/data/hash-generator/index.html
visual-qa-competitors/hash-generator`, viewed with the `Read` tool before recording this
verdict.

Re-verification of the earlier 2026-08-25 verdict, which predated the craft-retrofit's own
Phase 0+1 merge (commit `bc9d585`, 35 minutes after that verdict was recorded) - the density/
layout pass could plausibly have changed the outcome, so it was re-checked against the current
build rather than assumed to still hold. One real tooling bug found and fixed along the way:
`scripts/lineup-squint.js`'s competitor loader had no exclusion for a directory's own
previously-recorded `verdict-*.png` files, so the first re-run silently fed the OLD verdict
composite back in as two bogus extra "competitor" tiles. Fixed (`VERDICT_FILE_RE` exclusion)
before judging this verdict for real.

Competitors (real screenshots, non-full-page 1440x900, captured 2026-08-25 via
`scripts/capture-competitor-screenshot.js`, unchanged from the prior verdict):
`browserling-md5.png`, `codebeautify-md5.png`, `emn178-online-tools-sha256.png`,
`md5hashgenerator-com.png`.

## Lineup test: **PASS**

Still holds post-Phase-1. This repo's own tile reads as a single, uncluttered card centered on
one task (a labeled drop-zone with the dev-family ochre mark, plus one accent-filled action
button) against a plain background, now with the shared explorer-window path bar
("~ / developer / hash-generator") visible above it - an identity cue none of the four
competitors have. All four competitors still read differently at a glance: one is partly
obscured by a promo/pricing popup, one carries a dense sidebar-plus-nav developer-tools-
directory layout, one renders two mostly-empty columns, one pairs a dense right-sidebar
"Related Tools" list with body copy. The contrast in density and focus remains real and visible
without reading any label.

## Squint test: **PASS**

Blurred, the page still resolves to one dominant shape: the bordered drop-zone card with its
ochre mark and teal "Choose file" button, centered, high-contrast against the lighter
background. Heading text above blurs to a soft gray block reading as clearly secondary. One
dominant element survives the blur.
